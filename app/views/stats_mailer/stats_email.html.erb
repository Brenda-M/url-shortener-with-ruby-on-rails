<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>2-hour Analytics Insights</title>
  </head>
  <body
    style="font-family: 'Arial', sans-serif; background-color: #f4f4f4; color: #333; padding: 20px;"
  >
    <div
      style="max-width: 600px; margin: 0 auto; background-color: #fff; padding: 20px; border-radius: 10px; box-shadow: 0 0 10px rgba(0,0,0,0.1);"
    >
      <h2 style="color: #0066cc; margin-bottom: 20px;">
        Hello <%= @user.username %>,
      </h2>

      <p style="font-size: 18px; font-weight: bold; color: #0066cc;">
        Your Latest Link Insights
      </p>

      <% if @urls_info.present? %>
        <ul style="list-style: none; padding: 0; margin: 0;">
          <% @urls_info.each do |url_info| %>
            <li style="margin-bottom: 10px;">
              <strong>Original URL:</strong> <%=
                url_info[:original_url]
              %><br />
              <strong>Short URL:</strong> <%= url_info[:short_url] %><br />
              <strong>Click Count:</strong> <%= url_info[:click] %><br />
            </li>
          <% end %>
        </ul>
      <% end %>
      <p style="font-size: 14px; color: #666;">
        Thank you for using SnappyUrl!
      </p>
    </div>
  </body>
</html>
